# Ecommerce Back-End

# Para testar a api no Insomnia

O arquivo back-end-ecommerce-insomnia.json consta todos os endpoints

## Como rodar o projeto

`npm install`
`npm start`

## Como rodar os testes

`npm install`
`npx vitest run`
